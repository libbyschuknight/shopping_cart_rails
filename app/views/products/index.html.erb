
<h4>Welcome to faker.com </h4>

<h1>Products</h1>

<div id = products_list>
  <ul>
    <% @products.each do |product| %>
    <li>
      <h3> <%= product.name %> </h3>
      <p> <%= product.description %> </p>
      <p> <%= product.price %> </p>
   </li>
    <% end %>
  </ul>
</div>
